import template from './art-signup-invite.html';

import ArtSignupInviteController from './art-signup-invite.controller';

const artSignupInviteComponent = {
    template,
    controller: ArtSignupInviteController,
    controllerAs: 'signupCtrl',
};

export default artSignupInviteComponent;
